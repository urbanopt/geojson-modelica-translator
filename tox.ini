[tox]
envlist=
    python,
    flake8,
    docs,
skipsdist=True

[testenv:python]
deps=
    -r{toxinidir}/requirements.txt
commands=
    py.test
whitelist_externals=
    cp

[testenv:flake8]
basepython=python
deps=
    flake8
commands=flake8 {toxinidir}

[testenv:docs]
deps=
    -r{toxinidir}/requirements.txt
commands=
    python setup.py build_sphinx
whitelist_externals=
    make
    cp
